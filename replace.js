const fs = require('fs'); const file = 'd:/Dev/Nextjs Projects/Markify/components/home/FinalCTA.jsx'; const lines = fs.readFileSync(file, 'utf8').split('\n'); const start = lines.findIndex(l => l.includes('<motion.svg')); const end = lines.findIndex(l => l.includes('</motion.svg>')); if (start !== -1 && end !== -1) { lines.splice(start, end - start + 1, '                        <div className=\w-[126px] h-[126px] rounded-full bg-primary flex items-center justify-center\></div>'); fs.writeFileSync(file, lines.join('\n')); console.log('File written.'); } else { console.log('Not found'); }
